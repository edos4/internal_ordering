/ Category Section
%section#category.container
  #grocery
    %h4.section-title Everyday Grocery
    %ul.hs.full.no-scrollbar.category
      - @everydays.where(category: 'Grocery').each do |g|
        %li.item 
          .card
            %img.card-img-top{:src => g.image}/
            %p.card-title
              = g.name         
  #health
    %h4.section-title Everyday Health
    %ul.hs.full.no-scrollbar.category
      - @everydays.where(category: 'Health').each do |g|
        %li.item 
          .card
            %img.card-img-top{:src => g.image}/
            %p.card-title
              = g.name

          
  #booze
    %h4.section-title Everyday Booze
    %ul.hs.full.no-scrollbar.category
      - @everydays.where(category: 'Booze').each do |g|
        %li.item 
          .card
            %img.card-img-top{:src => g.image}/
            %p.card-title
              = g.name