%nav.navbar.navbar-expand-md.navbar-dark.fixed-top.arffogato-bg
	.container
		%a.navbar-brand{:href => "/store?messenger_id=#{params['messenger_id']}"}
			Arffogato
		= link_to "/store/view_cart/?messenger_id=#{params['messenger_id']}" do
			%button.btn.bg-primary.cart-quantity View Cart
				
:javascript
	$('#view_cart').click(function () {
		window.location = "/store/view_cart/?messenger_id=#{params['messenger_id']}"
	});
