.main.main-content.col-lg-10.p-0.offset-sm-1
  .main-content-container.container-fluid.px-4
    / Page Header
    .page-header.row.no-gutters.py-4
      .col-6.col-sm-4.text-center.text-sm-left.mb-0
        %span.text-uppercase.page-subtitle Overview
        %h3.page-title Bank Accounts
      .col-md-8
        = link_to 'New Bank account', new_bank_account_path, :class=>"mt-3 btn btn-sm btn-primary mr-1 float-right btn-create"
    .row
      .col
        %table.table.table-striped.table-bordered{:role => 'datatable'}
          %thead
            %tr
              %th Name
              %th Actions

          %tbody
            - @bank_accounts.each do |bank_account|
              %tr
                %td= bank_account.name
                %td
                  = link_to '', edit_bank_account_path(bank_account), :class => "far fa-edit"
                  = link_to '', bank_account, method: :delete, data: { confirm: 'Are you sure?' }, :class => "far fa-trash-alt"

:javascript
  $(document).ready(function() { $('#example').DataTable(); } );
  